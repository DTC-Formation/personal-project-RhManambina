import 'package:flutter/material.dart';

class Filiere extends StatelessWidget {
  const Filiere({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Département'),
    );
  }
}
